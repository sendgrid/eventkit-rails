<nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
	<!-- Brand and toggle get grouped for better mobile display -->
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>

			{{#link-to 'home' class="navbar-brand"}}
				<img alt="Brand" src="/sendgrid-logo.svg" height="34px">
			{{/link-to}}
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse navbar-ex1-collapse">
			<ul class="nav navbar-nav">
				{{#link-to 'home' tagName='li'}}<a href="#" data-toggle="collapse" data-target=".navbar-collapse">HOME</a>{{/link-to}}
				{{#link-to 'settings' tagName='li'}}<a href="#" data-toggle="collapse" data-target=".navbar-collapse">SETTINGS</a>{{/link-to}}
				{{#link-to 'detailedSearch' tagName='li'}}<a href="#" data-toggle="collapse" data-target=".navbar-collapse">DETAILED SEARCH</a>{{/link-to}}
			</ul>
			<form class="navbar-form navbar-right" role="search" {{action 'search' on='submit'}}>
				<div class="form-group">
					{{input type="text" value=query class="form-control" placeholder="Search"}}
				</div>
			</form>
		</div>
	</div><!-- /.navbar-collapse -->
</nav>

<div class="container" role="main">
	{{outlet}}
</div>