# ==========================================================================
# Home Controller 
# ==========================================================================
# The controller for the home page (dashboard). This is a subclass of 
# EventKit.WildcardSearchController, which implements the wild card
# search actions for th emain search field on the home view.
#

EventKit.HomeController = EventKit.WildcardSearchController.extend()